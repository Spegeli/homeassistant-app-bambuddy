configuration:
  debug:
    name: Debug
    description: Flag to enable debug messages
  trusted_frame_origins:
    name: Trusted Frame Origins
    description: Comma-separated list of URLs allowed to embed BamBuddy in an iFrame (e.g. http://homeassistant.local:8123). Leave empty if not needed.
network:
  8000/tcp: Web Interface
