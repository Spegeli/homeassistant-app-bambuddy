configuration:
  debug:
    name: Debug
    description: Flag to enable debug messages
  trusted_frame_origins:
    name: Allowed Origin URLs
    description: URLs that are allowed to embed BamBuddy inside Home Assistant (e.g. your HA address). Multiple entries can be added.
network:
  8000/tcp: Web Interface
